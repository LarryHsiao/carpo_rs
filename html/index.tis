function self.ready(){
    view.load_files();
    $(#search_input).on("change", search);
}

function append_file(file_name){
    $(div#list > select).$append(<option>{ file_name }</option>);
}

function search(){
    view.search($(#search_input).text)
}

function clear_files(){
    $(div#list > select).clear()
}